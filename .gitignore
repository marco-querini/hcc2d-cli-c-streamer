# Local build artifacts
hcc2d_streamer
*.o
release/
*.deb
*.build
*.buildinfo
*.changes
*.dsc
*.tar.*

# Debian packaging work files
debian/.debhelper/
debian/debhelper-build-stamp
debian/files
debian/hcc2d-streamer/
debian/*.debhelper.log
debian/*.substvars
debian/tmp/

# Temporary local output
dist/
apt-repo/tmp/
*.gif
*.gif.tmp.*

# Retained locally; protocol documentation is not published yet
PROTOCOL.md

# Generated APT repository content
apt-repo/public/*

# A public archive key may be published deliberately.
!apt-repo/public/hcc2d-archive-keyring.gpg

# Private signing material and local GnuPG state
.secrets/
.gnupg/
gnupg/
*.private-key
*.secret-key
*.pem
*.key
*.p12
*.pfx
*.jks
*.keystore
*.kdbx
*.mobileprovision
id_rsa
id_rsa.*
id_dsa
id_dsa.*
id_ecdsa
id_ecdsa.*
id_ed25519
id_ed25519.*
secring.gpg
pubring.kbx
trustdb.gpg
random_seed
S.gpg-agent*
private-keys-v1.d/
openpgp-revocs.d/

# Local credentials and environment files
.env
.env.*
!.env.example
credentials
credentials.*
*.credentials
*.token
.aws/
.config/gcloud/

# Editor and operating-system files
.DS_Store
*.swp
*~
